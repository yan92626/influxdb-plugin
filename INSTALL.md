# InfluxDB3 Head 安装使用说明

一个 Chrome 浏览器扩展，用于可视化查看和查询 InfluxDB 3.x 数据库（类似 es-head 之于 Elasticsearch）。

## 一、安装（约 1 分钟）

1. 把收到的 zip 解压到一个**固定的文件夹**（例如 `~/chrome-extensions/influxdb3-head/`）
   - ⚠️ 加载后这个文件夹不能删除或移动，否则扩展会失效
2. Chrome 地址栏打开 `chrome://extensions`
3. 打开右上角的 **「开发者模式」** 开关
4. 点左上角 **「加载已解压的扩展程序」**，选择刚才解压出来的文件夹
5. 完成。点击浏览器工具栏的扩展图标（可能收在 🧩 拼图图标里，可以点📌固定）即可打开

> Chrome 重启时可能弹「请停用以开发者模式运行的扩展程序」提示，点「保留」即可，属正常现象。

## 二、首次配置连接

首次打开会自动弹出「连接管理」：

| 字段 | 说明 | 示例 |
|---|---|---|
| 名称 | 随意起，用于区分多个环境 | `生产-监控` |
| 地址 | InfluxDB 3 的 HTTP 地址，**带 http:// 或 https://** | `https://influxdb.example.com` |
| Token | InfluxDB 3 的 API token（`apiv3_` 开头的完整值，**不要带 Bearer 前缀**） | `apiv3_xxxx...` |
| 默认数据库 | 可选；**只读 token 强烈建议填写**（无列库权限时将只显示这个库） | `monitoring` |

填完先点 **「测试连接」**，看到 ✅ 和版本号再点「保存」。可以保存多个连接，顶栏下拉随时切换。

## 三、功能速览

- **工作区**（主界面）：左侧是数据库→表→列的树形导航
  - **点表名**：自动查询该表最近数据并展示（时间范围可在工具栏「预览范围」调整）
  - **展开表**：查看列结构（🕐 时间列 / 🏷 标签列 / 📈 字段列）
  - 上方编辑器手写 SQL 或 InfluxQL（下拉切换语言），可选择 IANA 时区，`⌘/Ctrl + Enter` 执行
  - 结果可切换 **表格 / 折线图**，可导出 **CSV / JSON**，「历史查询…」可回填最近 50 条
- **概览**：实例健康状态、版本、每个库的表数量
- **写入**：粘贴 Line Protocol 文本写入数据（需要有写权限的 token）
- **管理**：创建/删除数据库（需要 admin token；删除要输入库名二次确认）

## 四、查询防护（自动生效）

为避免拖垮数据库，扩展会在执行前做以下防护：

- SELECT 没写 `LIMIT` 时自动追加上限（工具栏「最大行数」可调，默认 1000）
- 查询**没有时间范围条件**时会弹窗确认——大库请务必带上类似
  `WHERE time >= now() - INTERVAL '1 hour'` 的条件
- `DROP` / `DELETE` 等修改类语句执行前需确认

## 五、常见问题

**Q: 测试连接报「Token 无效或权限不足」/ invalid token？**
确认粘贴的是 token 创建时显示的完整原始值（`apiv3_` 开头），不是控制台里打码的展示值；不要带 `Bearer ` 前缀。

**Q: 左侧树显示「token 无列库权限」？**
你的 token 是只读/受限 token，无法调用管理接口列库。在连接配置里填「默认数据库」即可正常使用。

**Q: 查询报 file limit / parquet files 错误？**
库太大且查询没限定时间范围。加上时间条件（如最近 1 小时）即可；点表预览时可在「预览范围」下拉里调小范围。

**Q: 支持 InfluxDB 1.x / 2.x / Cloud 吗？**
不支持，仅支持自建 InfluxDB 3.x（Core / Enterprise）。

**Q: 我的 token 安全吗？**
Token 保存在你本机 Chrome 的扩展存储里，不会上传到任何地方；扩展只与你配置的 InfluxDB 地址通信。请勿在共享电脑上保存生产 token。
