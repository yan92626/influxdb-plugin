# Changelog

本项目的所有重要变更都会记录在此文件中。

格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [Semantic Versioning](https://semver.org/lang/zh-CN/)。

## [Unreleased]

## [0.2.0] - 2026-07-30

### Added

- 时序图表支持选择数值指标、按非数值维度拆分序列，并按最后值、平均值、最小值或最大值聚合。
- 时序图表支持自动及固定时间粒度、序列显隐、统计图例、悬停明细、拖拽与滚轮缩放。
- 图表展示原始行数、时间点数、序列数、重叠采样聚合、高基数截断和无效数据提示。

### Changed

- 图表根据结果区域自适应尺寸，稠密数据默认隐藏采样点，仅在低密度或悬停时突出数据点。
- 表预览查询遵循“最大行数”配置，不再固定限制为 100 行。
- 发布流程要求每次 bug 修复或功能新增同步更新 Changelog，并在用户确认后才能创建和推送 tag。

### Fixed

- 修复不同 Tag 数据在相同时间点被错误连接成一条折线的问题。
- 修复重复时间点产生竖线的问题，同一序列和时间桶内的数据会按所选规则聚合。
- 修复图表横轴未明确遵循所选 IANA 时区的问题，横轴刻度、轴标题和悬停时间现在使用同一时区。
- 修复纳秒精度及无时区后缀的 InfluxDB 时间字符串可能无法生成图表的问题。

## [0.1.1] - 2026-07-29

### Added

- 查询页面支持选择并持久化 IANA 时区。
- 查询历史记录所使用的时区，并在回填查询时恢复。
- Release 打包生成 zip 与 SHA-256 校验文件。

### Fixed

- 将 InfluxDB SQL 返回的无时区后缀时间戳按 UTC 解析。
- 切换时区时立即基于原始数据刷新结果，避免重复转换造成累计偏移。
- 自动追加的 `LIMIT` 保持在 InfluxQL `tz()` 子句之前。

## [0.1.0] - 2026-07-27

### Added

- Chrome Manifest V3 扩展基础框架。
- InfluxDB 3 Core / Enterprise 多连接管理与健康检查。
- 数据库、表和字段浏览，支持受限 token 的默认数据库降级。
- SQL 与 InfluxQL 查询编辑器、历史记录和查询防护。
- 表格与时序折线图展示，以及 CSV / JSON 导出。
- Line Protocol 写入和数据库创建、删除管理。
